{
  "description": "This file is required for running on Heroku",
  "scripts": {
    "postinstall": "npm install --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": "18.*",
    "npm": "9.*"
  }
}
